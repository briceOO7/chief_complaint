# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# PHI data — NEVER commit anything in data/raw/
data/raw/*
!data/raw/README.md

# Results (may contain PHI on PHI machine)
data/results/*
!data/results/.gitkeep

# v2 gold-label reconciliation workspace (PHI-adjacent — produced on the PHI
# machine from real medevac data; never commit). See docs/PHI_MACHINE_SETUP.md.
data/gold_standard_v2_phi/

# Model weights
models/*
!models/.gitkeep

# Secrets and machine-local config
.env
local_paths.cfg

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/
